import * as React from 'react';

//esto es lo uniuco que necesitamos para este file inicializar el create context
const sharepointContext = React.createContext(undefined);

export default sharepointContext